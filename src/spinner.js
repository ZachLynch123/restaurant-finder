const spinner = (props) => {
    let stillLoad = true;

    if (stillLoad) {
        <Loader className="loading"
                  type="Puff"
                  color="#191970"
                  height="100"
                  width="100"
                />
    }
}